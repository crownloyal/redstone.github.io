import graphHelper from 'red-stone/utils/graph-helper';
import { module, test } from 'qunit';

module('Unit | Utility | graph helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = graphHelper();
  assert.ok(result);
});
